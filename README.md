# Malkuth Control Hub v3.1

Visual release of the Malkuth Control Hub.

Core flow:

**Intention → Resolve → Mandate → Execute → Verify → Learn**

The visual master is preserved as the interface. The central phone controls are overlaid with tappable interaction zones for mobile use.

## GitHub Pages
Enable Pages from the repository root/main branch. `index.html` is ready to serve directly.
